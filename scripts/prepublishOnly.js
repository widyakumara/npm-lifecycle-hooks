#!/usr/bin/env node

/**
 * prepublishOnly
 * Run BEFORE the package is prepared and packed, ONLY on npm publish. (See below.)
 */

console.log('\n🚩 this is: prepublishOnly\nRun BEFORE the package is prepared and packed, ONLY on npm publish. (See below.)\n');