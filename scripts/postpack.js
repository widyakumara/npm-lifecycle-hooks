#!/usr/bin/env node

/**
 * postpack
 * Run AFTER the tarball has been generated and moved to its final destination.
 */

console.log('\n🚩 this is: postpack\nRun AFTER the tarball has been generated and moved to its final destination.\n');