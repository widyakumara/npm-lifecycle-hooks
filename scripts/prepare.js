#!/usr/bin/env node

/**
 * prepare
 * Run both BEFORE the package is packed and published, on local npm install without any arguments, and when installing git dependencies . This is run AFTER prepublish, but BEFORE prepublishOnly.
 */

console.log('\n🚩 this is: prepare\nRun both BEFORE the package is packed and published, on local npm install without any arguments, and when installing git dependencies . This is run AFTER prepublish, but BEFORE prepublishOnly.\n');