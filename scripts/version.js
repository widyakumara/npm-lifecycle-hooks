#!/usr/bin/env node

/**
 * version
 * Run AFTER bumping the package version, but BEFORE commit.
 */

console.log('\n🚩 this is: version\nRun AFTER bumping the package version, but BEFORE commit.\n');